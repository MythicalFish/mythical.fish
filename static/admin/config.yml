backend:
  name: github
  repo: MythicalFish/mythical.fish

media_folder: static/assets
public_folder: /assets

collections:
  - name: project
    label: Project
    folder: static/content/projects
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - {label: Title, name: title, widget: string}
      - {label: Body, name: body, widget: markdown}
      - {label: Tags, name: tags, widget: list, fields: [{label: URL, name: url, widget: text}, {label: Label, name: label, widget: text}]}
      - {label: Links, name: links, widget: list, fields: [{label: URL, name: url, widget: text}, {label: Label, name: label, widget: text}]}
      - {label: Position, name: position, widget: number, default: 0}